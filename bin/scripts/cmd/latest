#!/usr/bin/env bash

branch=$(git rev-parse --abbrev-ref HEAD)
tag="latest"

git tag -fa ${tag} && git push origin ${1}
echo "Branch: ${branch}"
created=$(git describe --abbrev=0 --tags)
echo "New tag: ${created}"
